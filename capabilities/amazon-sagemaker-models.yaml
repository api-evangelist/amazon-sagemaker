naftiko: 1.0.0-alpha2
info:
  label: Amazon SageMaker API — Models
  description: 'Amazon SageMaker API — Models. 3 operations. Lead operation: Amazon SageMaker Create a Model. Self-contained
    Naftiko capability covering one Amazon Sagemaker business surface.'
  tags:
  - Amazon Sagemaker
  - Models
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_SAGEMAKER_API_KEY: AMAZON_SAGEMAKER_API_KEY
capability:
  consumes:
  - type: http
    namespace: amazon-sagemaker-models
    baseUri: https://api.sagemaker.{region}.amazonaws.com
    description: Amazon SageMaker API — Models business capability. Self-contained, no shared references.
    resources:
    - name: '#CreateModel'
      path: /#CreateModel
      operations:
      - name: createmodel
        method: POST
        description: Amazon SageMaker Create a Model
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: '#DescribeModel'
      path: /#DescribeModel
      operations:
      - name: describemodel
        method: POST
        description: Amazon SageMaker Describe a Model
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: '#ListModels'
      path: /#ListModels
      operations:
      - name: listmodels
        method: POST
        description: Amazon SageMaker List Models
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: amazon-sagemaker-models-rest
    port: 8080
    description: REST adapter for Amazon SageMaker API — Models. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/createmodel
      name: createmodel
      description: 'REST surface for #CreateModel.'
      operations:
      - method: POST
        name: createmodel
        description: Amazon SageMaker Create a Model
        call: amazon-sagemaker-models.createmodel
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/describemodel
      name: describemodel
      description: 'REST surface for #DescribeModel.'
      operations:
      - method: POST
        name: describemodel
        description: Amazon SageMaker Describe a Model
        call: amazon-sagemaker-models.describemodel
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/listmodels
      name: listmodels
      description: 'REST surface for #ListModels.'
      operations:
      - method: POST
        name: listmodels
        description: Amazon SageMaker List Models
        call: amazon-sagemaker-models.listmodels
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: amazon-sagemaker-models-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon SageMaker API — Models. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: amazon-sagemaker-create-model
      description: Amazon SageMaker Create a Model
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: amazon-sagemaker-models.createmodel
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: amazon-sagemaker-describe-model
      description: Amazon SageMaker Describe a Model
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: amazon-sagemaker-models.describemodel
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: amazon-sagemaker-list-models
      description: Amazon SageMaker List Models
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: amazon-sagemaker-models.listmodels
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
